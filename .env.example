# SQL Server SA password used by docker-compose
SA_PASSWORD=Your_password123

# Exposed ports for local containers
SQL_PORT=1433
API_PORT=5200
WEB_PORT=5173

# Frontend API base URLs
# Used when running Vite locally
VITE_API_BASE_URL=http://localhost:5200
# Used when building the docker image (overrides build arg)
DOCKER_VITE_API_BASE_URL=http://api:5200
